
## t_6a9f4ed4-c — Sub-card C: Search + Click-to-Focus (2026-08-24)

- BRANCH=feat/portfolio-c (from feat/portfolio-b @ 34f7647)
- COMMIT=df591a3 (6 commits, feat/portfolio-b..HEAD)
- npm test: Test Files 2 passed (2) / Tests 35 passed | 6 skipped (41) — baseline preserved
- eslint: exit 0 (clean), repo-wide `eslint .`
- next build: ✓ Compiled successfully
- e2e (new): npx playwright test → 4 passed ('/' focus; capped dropdown + Enter select + DetailPanel + data-focus-active; pixel-diff camera-moved proof; Escape clears query+selection)
- Scope: only files-in-scope touched — SearchBox.tsx (new), GraphViewer.tsx, e2e/ specs+fixture (new), playwright.config.ts (new), package.json (+@playwright/test devDep + test:e2e script), vitest.config.ts (new: scope vitest to src unit tests so it no longer sweeps Playwright specs), .gitignore (playwright artifacts). graphLoader.ts, adjacency.ts, AppHeader/StatsBar/FileDrop/CommunityLegend, DetailPanel, and all existing tests untouched.

### Drift from plan (documented, not blocking)
1. Fixture shape: plan sketched nodes as {name,kind,file,loc,val,color}; the actual loader contract reads {label,type,community_name,source_file,source_location} and derives val/color. Fixture written in the loader's real shape (loader is read-only/test-locked).
2. vitest.config.ts added (not in plan's file list): without it vitest's default glob collected e2e/*.spec.ts and failed the suite ("Playwright Test did not expect test() here"). Existing tests unmodified.
3. Plan's `.graph-backdrop ~ canvas` selector assumed canvas is a backdrop sibling; react-force-graph nests its canvas in a wrapper div — spec targets `main canvas` directly.
4. Input exposes role="combobox" (correct input+listbox ARIA pattern); spec locates it via that role.
5. rAF retry bounded at ~120 frames per plan's own risk note.
